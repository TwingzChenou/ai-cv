{
    "name": "project-cv-ia",
    "version": "1.0.0",
    "description": "CV Interactif avec IA - Backend Node.js + Frontend React",
    "scripts": {
        "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
        "start": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
    },
    "keywords": [
        "cv",
        "ia",
        "react",
        "langchain"
    ],
    "author": "Quentin Forget",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "dependencies": {
        "@google/generative-ai": "^0.24.1",
        "cors": "^2.8.5"
    }
}
